{% extends 'base.html.twig' %}

{% block content %}
<div class="wrapper">
    <div class="row">
        <div class="panel panel-success">
            <div class="panel-heading">
                <h1 class="header-title">AAROGYA MED LLC <span>{#{{ name }}#}</span></h1>
            </div>
            <div class="panel-body container">
                <table class="table table-hover order">
                    <thead>
                    <tr>
                        <th><span><a href="#">Product Name</a></span></th>
                        <th><span>Size</span></th>
                        <th><span>Price</span></th>
                        <th><span>Count</span></th>
                        <th><span>Cost</span></th>
                        <th><span>Actions</span></th>
                    </tr>
                    </thead>
                    <tbody>

                    </tbody>
                    <tfoot>
                    <tr>
                        <td class="total-count" colspan="2"><span>Selected count </span> 0</td>
                        <td class="total-price" colspan="2"><span>Selected price </span> 0$</td>
                        <td class="create_invoice" colspan="2"><a href="{{ path('submit_order')}}" class="btn btn-success">Submit order</a></td>
                    </tr>
                    </tfoot>
                </table>
            </div>
        </div>
    </div>
</div>
    {% endblock content %}
    {% block javascripts %}
        {{ parent() }}
        <script type="text/javascript" src="{{ asset('bundles/app/js/main.js') }}"></script>
    {% endblock %}
