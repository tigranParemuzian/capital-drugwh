{% if admin.isGranted('EDIT', object) and admin.hasRoute('edit') %}
    <a href="{{ path('packing_slip', {'invoiceNumber':object.number}) }}" target="_blank" class="btn btn-default calculate_link" title="{{ 'action_calculate'|trans({}, 'SonataAdminBundle') }}">
        <span class="glyphicon glyphicon-floppy-disk"></span>&nbsp;PS
    </a>
{% endif %}