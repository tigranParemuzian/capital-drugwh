{% if admin.isGranted('EDIT', object) and admin.hasRoute('edit') %}
    <a href="{{ path('send_email', {'invoiceNumber':object.number}) }}" class="btn btn-sm btn-default calculate_link" title="{{ 'action_calculate'|trans({}, 'SonataAdminBundle') }}">
        <span class="glyphicon glyphicon glyphicon-envelope"></span>&nbsp;Email
    </a>
{% endif %}